# Mihomo Smart Selector 界面规范

现有工具界面的增量打磨。使用 Impeccable 的 Operate 模式：让用户快速确认当前状态、
比较节点证据并完成明确的操作。蓝色强调色、侧栏导航和明暗主题延续现有设计，界面支持简体中文与英文。

## 视觉变量

变量定义集中在 `web/src/style.css`。浅色主题采用偏冷的浅底与白色内容面，
深色主题采用蓝灰背景与分层内容面。文字、边界、交互和状态各有语义变量。

- `--bg` / `--surface` / `--surface-muted`：页面、内容、次级背景。
- `--text` / `--muted`：正文与次级说明，保持可读对比度。
- `--blue` / `--soft`：当前选择、可操作链接和对应的浅色背景。
- `--primary` / `--primary-hover` / `--on-primary`：主按钮及其文字。
- `--green` / `--good-bg`：完成、健康；`--red` / `--bad-bg`：失败、不可用。
- `--warning` / `--warning-bg`：需要关注；待开始、停止和未知状态使用中性色。
- `--line` / `--control-border`：容器分隔与输入控件边界。
- `--radius-sm` / `--radius-md`：控件 8px、内容容器 12px。
- `--space-1` 到 `--space-6`：4、8、12、16、24、32px。

字体使用本机 Segoe UI / 苹方 / 微软雅黑等中英文字体回退，不依赖远程字体请求。
正文与操作文字主要使用 14px；次级说明 12–13px。手机输入控件至少 16px，
高频触摸操作至少 44px 高。数值比较使用等宽数字，节点名称与 Provider 允许换行。

## 页面与组件职责

- `App.vue`：应用外壳、导航、语言与主题切换、确认弹窗，继续组合既有业务视图。
- `ScanWorkbench.vue`：扫描配置、进度、结果与候选详情的组合；业务状态仍由 `useWorkbench` 提供。
- `CandidateDetails.vue` / `CandidateActions.vue`：选中节点的证据、比较与确认操作。
- `MonitoringPage.vue`：监控方案、观察窗口、页签和操作状态。
- `MonitorNodeList.vue`：监控排名的桌面表格与手机节点列表；接收类型化 props，向父组件发出查看与复测事件，不发 API 请求。
- 既有趋势、诊断、分页组件保持原有职责；不迁移 Hash 路由或状态管理。

## 布局与交互

桌面保留左侧导航和工作区。扫描配置与结果分区明确；主要扫描按钮在首次与再次扫描时
保持同一视觉权重。候选详情是辅助阅读区，不能长期挤压排名的关键列。

手机保留五个导航入口，在一行内完整显示；排名以可阅读的节点条目组织，
候选详情沿用原生 dialog 抽屉。监控的成功率、P95、状态、故障证据和复测操作无需横向寻找。
弹窗与抽屉限制自身滚动并留出安全区。

键盘用户可以跳至主内容，页签沿用方向键切换，焦点清晰可见。
图标辅助文字传达意义；颜色不能作为唯一的状态提示。
空结果说明原因和下一步，搜索无结果提供清空操作；失败仍保留已有证据与重试入口。

仅对状态变化添加短过渡。进度条使用 transform 更新，遵循减少动态效果偏好。
不添加装饰性动画、虚构业务指标或新的运行时依赖。

## 验证范围

通过隔离的 mock Controller 与临时数据库验证真实扫描路径；监控历史使用现有确定性
浏览器 fixture。覆盖桌面、手机、明暗主题、长名称、空结果、失败恢复与键盘操作。
截图中的示例数据不能解读为真实节点测速或真实路由器监控结果。

## 中英文适配

工作区右上角与 LAN token 连接页提供「简体中文 / English」选择器。首次访问按浏览器语言列表
选择首个支持的语言；没有中文或英文时使用简体中文。手动选择保存在当前浏览器的
`mss-locale`，优先于浏览器语言，并同步同源的其他标签页。浏览器禁用本地存储时，仍可在当前页切换。

切换语言立即更新文案与根元素 `lang`，保留当前路由、扫描、表单草稿及按标识保存的筛选条件。
手机英文导航使用 Scan / Monitor / Nodes / History / Settings 短标签；表单和表头允许换行。
日期、地区名称、数值与列表通过 `Intl` 格式化，日期继续使用浏览器的本地时区。

翻译入口为 `web/src/i18n.ts`，英文目录为 `web/src/i18n/messages.ts`。中文原文作为消息键，
新增文案使用 `t(source, params)`，完整句子中的变量用占位符表达。不要翻译节点、策略组、
Provider 的名称、URL、ID 或用户填写的内容；也不要把翻译后的文字用作业务判断条件。
异步通知保存源文案，在显示时调用 `translateMessage`，让已有通知跟随语言切换。
已知后端状态与错误有显式映射，未识别的诊断信息保留原文；下载诊断包的内容由后端独立生成。

`pnpm --dir web test` 检查语言选择、响应式更新、占位符、名称保留与格式化；
`pnpm --dir web test:e2e` 覆盖浏览器偏好、持久化、跨标签更新、扫描确认、监控证据、
320px 布局和禁止本地存储时的 LAN 连接页。发布前仍需运行嵌入式资源一致性检查。
